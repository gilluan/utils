```bash

  sudo apt-get install cntlm
  sudo cp cntlm.conf /etc
  sudo vi cntlm.conf
  sudo emacs cntlm.conf
  sudo /etc/init.d/cntlm
  sudo /etc/init.d/cntlm restart
  vi /etc/cntlm.conf
  sudo vi /etc/cntlm.conf
  cat cntlm.conf
  sudo cat cntlm.conf
  systemctl status cntlm.service
  sudo nano cntlm.conf
 sudo /etc/init.d/cntlm restart
 sudo nano /etc/cntlm.conf
 emacs /etc/cntlm.conf
 sudo emacs /etc/cntlm.conf
 sudo /etc/init.d/cntlm restart


```
